// Fill these in from your Supabase project (Project Settings → API),
// then redeploy. The anon key is safe to expose in client code — it is
// designed to be public, and Row Level Security (see schema.sql) is what
// actually restricts who can read or write your data.
window.SUPABASE_URL = "YOUR_SUPABASE_URL";
window.SUPABASE_ANON_KEY = "YOUR_SUPABASE_ANON_KEY";
